<div class="card-header">
    <a href="{{ $url }}" data-animation="bounce" data-toggle="{{ $dto }}"
        data-target="{{ $dta }}" class="btn btn-sm btn-primary float-right"><i class="mdi mdi-plus"></i>Tambah
        Data</a>
</div>
